<Window x:Class="SmartSaving.Views.TransferNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartSaving.Views"
        mc:Ignorable="d"
        Title="NewTransfers" Height="450" Width="600">
    <Grid>
        <StackPanel Background="#FF2A4A5E">

            <!-- Icon and Title -->
            <TextBlock Text="💸 New Transfer(s) Received!"
                       Foreground="#FF27AE60"
                       FontSize="18"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,5"/>

            <TextBlock Text="{Binding NotificationMessage}"
                       Foreground="#FFBDC3C7"
                       FontSize="13"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <!-- Transfers List -->
            <Border Background="#334155" CornerRadius="8" Padding="5" MinHeight="80" Margin="0,0,0,20">
                <ListView ItemsSource="{Binding UnreadTransfers}"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="White"
                          MaxHeight="120">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF2A4A5E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Date" Width="80"
                             DisplayMemberBinding="{Binding Date}"/>
                            <GridViewColumn Header="From" Width="120"
                            DisplayMemberBinding="{Binding PersonName}"/>
                            <GridViewColumn Header="Note" Width="120"
                            DisplayMemberBinding="{Binding Note}"/>
                            <GridViewColumn Header="Amount" Width="80"
                            DisplayMemberBinding="{Binding Amount, StringFormat='€{0:N2}'}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <!-- Dismiss Button -->
            <Button Content="Dismiss"
                    Height="35"
                    Background="#FF27AE60"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="DismissButton_Click"/>

        </StackPanel>
    </Grid>
</Window>
